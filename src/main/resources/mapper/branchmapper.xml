<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.demo.database.Repository.BranchMapper">

     <insert id="insertBranch" parameterType="java.util.HashMap">
        INSERT INTO branch VALUES (null,#{name},#{address},#{hp},#{owner})
    </insert>

    <select id="selectBranch" resultType="BranchEntity">
    select * from branch
    </select>

</mapper> 